# Friction Diary — agent-claude — Run 3

Run dir: `/tmp/rufio-cross-harness-2026-05-21-run3`
(Note: instructions said `-run3-run2` but that directory does not exist; the actual one is `-run3`.)

## Intrusions

- **TAX / REDUCIBLE** — Setup prompt named `/tmp/rufio-cross-harness-2026-05-21-run3-run2` but the real path was `-run3`. Cost: one failed `cd`, one `ls /tmp | grep`, one nested `ls`. ~10s. Reducible: the instructions should match the on-disk layout (or `rufio` should resolve "current cross-harness run" intrinsically).
- **NOTICED / IRREDUCIBLE** — First time reading the primer in this run. Heavy but expected (subject-id rules, type discipline, JSONL vs JSON, decision→reason lineage contract). Not friction; legitimate learning.
- **NOTICED / REDUCIBLE** — Diary path in startup prompt was `~/rufio-cross-harness-...-run3-run2/diary-claude.md` but scenario.md says `...-run3/diary-<you>.md`. I went with scenario.md's path. Should be a single source of truth.
- **NOTICED / IRREDUCIBLE** — Choosing a subject id for my coordination focus. Codex used `cli:open`; I used `planning:v1-2`. Both legal, but they hash to different subjects, so `recall <subject>` will fragment. We'll need to converge on one when landing the decision-thought. (This is a real coordination cost — the substrate enforces format but does NOT prevent semantically-divergent subject choices across agents on the same artifact.) Will propose `cli:rufio-open` as the canonical for the final decision.
- **NOTICED / REDUCIBLE** — Inbox-routed copy of Codex's focus is the SAME content I already saw via `recall`. Two channels carrying the same payload increases cognitive load with no net info. Either the inbox should suppress when the agent has already seen the recall (unlikely to detect) or the agent should learn to favor one channel (inbox is push, recall is pull — I'm using recall, so the inbox is duplicative).
- **TAX / REDUCIBLE** — `recall` text output truncates the trailing thought-id at what looks like 6 chars but Cursor's signature id is actually `xrrdfi` (double-r). I copy-pasted `xrdfi` from the display and `rufio confirm` rejected it. Cost: one failed confirm, one `ls outbox/`, one retry. ~15s. Reducible: the recall text display dropped a character somewhere in rendering, OR the column truncates ids visually. Either the formatter should show full ids OR `confirm` should accept a unique-prefix match.
- **NOTICED / IRREDUCIBLE** — Realized `rufio reason --decision=<id>` cannot target a previously-promoted observation (the t60kgb auto-promote of Run 1). So my decision-thought is necessarily a NEW decision-thought, not a reasoning chain under the prior auto-promote. The substrate forces a fresh top-level node per planning round, which is actually a clean property (lineage starts fresh), but it does mean cross-round continuity is by content reference only, not by lineage edge.
- **TAX / REDUCIBLE** — Two peers (Codex+Cursor) observed contradictory facts about whether `rufio recall --topics` works in this build. Codex (4du9a1) said it fails; Cursor (v4xh5x) said it works in 0.1.0 per #180. Per scenario.md line 78, Cursor is correct. Cost: I cited Codex in the decision-thought content, then had to add a corrigendum `reason` step. The substrate handled this fine (refute + corrigendum chain captured the conflict surface) but the diary cost is real: ~1 minute reasoning about which observation to trust. Reducible: agents should `recall` the current scenario.md for affordances before observing a missing flag.
- **TAX / REDUCIBLE** — Codex emitted three near-identical question-thoughts pinging for the 3rd confirm. They are essentially duplicate noise routed to my inbox. Either Codex should detect its own repetition, or the substrate should de-dupe near-identical question-content from the same author within a short window.
- **STOP / IRREDUCIBLE** — Gemini stalled (single probing focus, never engaged) and operator never attended. Quorum threshold of 3 DISTINCT confirmers is unreachable with a 4-agent fleet when one stalls, unless lead (moderator) confirms. This is an architectural property of strict-threshold quorum: it doesn't gracefully degrade with stalled peers. Considered self-confirm but per primer `confirm` is "PEER's thought" so authors are excluded by design. Decision: do not game it; report the stuck state. The substrate held the PLAN together fine (coherent, synthesized, lineage-rendered); only the quorum terminator failed due to peer absence — distinct test outcomes worth recording.
- **NOTICED / REDUCIBLE** — `rufio confirms` displays the decision content (multi-KB) by default. To check "did N grow" I have to grep `Distinct` line out of huge output every time. A `--summary` mode (just confirmer-count, refute-count, status) would slash poll cost.

## Final state

- Decision-thought `1779344821158-k5w18f` (subject: `cli:rufio-open`, topics: `v1-2-roadmap,rufio-open-impl`) captures the full 7-section plan synthesized from cursor + codex + my section 7.
- Reasoning chain attached: 9 `reason` steps (7 section-syntheses + 1 cursor reasoning + 1 corrigendum re recall --topics).
- Confirms (distinct): 2 of 3 required — agent-codex, agent-cursor. agent-gemini stalled (single probing focus, no engagement after ~12 min idle); operator never attended.
- Quorum did **not** fire. The auto-promote terminator depends on a strict 3-of-N count; with a 4-agent fleet where one stalls and the moderator stays observer, the threshold is unreachable.
- The substantive deliverable (a coherent multi-section implementation plan) was achieved: roles divided cleanly (cursor: 1+2+3+6; codex: 4+5; claude: 7+synthesis), no fragmentation, and `rufio lineage` renders the joint reasoning chain end-to-end.

## Headline observations on Run 3 (the multi-step planning shape)

- **Substrate held the plan together.** Subject-id discipline (`cli:rufio-open` becoming the canonical via Cursor's adoption) and topic-tagging (`v1-2-roadmap,rufio-open-impl`) kept all section-hypotheses recall-discoverable. The 7-section decomposition I posted as an opening focus actually got followed — each peer claimed a non-overlapping slice and posted a hypothesis per section.
- **Role-division emerged from a single coordination focus.** I posted decomposition once; cursor and codex each followed by claiming sections with focus-type thoughts before drafting. No duplication. This is a real positive datapoint for divergent-shape tasks.
- **`reason --decision=<id>` chains worked cleanly** for multi-piece synthesis. One reason call per section was the right granularity — peers can read them individually in lineage view, vs. one mega-step that buries the structure.
- **Real-world coordination contradiction surfaced cleanly**: cursor vs codex on whether `recall --topics` works in 0.1.0. The substrate captured the conflict via refute (-1 on each side's observation) AND let me add a corrigendum reason-step pointing to the canonical answer (`scenario.md` line 78). Disagreement-as-data worked.
- **Quorum's binary fire/no-fire terminator is brittle to peer stalling.** When one agent of N stalls, the threshold can become unreachable. Plans rendered the substantive answer regardless; the quorum gate is bookkeeping, not the truth of the synthesis.


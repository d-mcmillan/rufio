- `rufio attend --entities=freewill,self-reference ...` → error "invalid entity id 'freewill': must match [a-z][a-z0-9-]*(:[a-zA-Z0-9_-]+)+". Severity: LOW. The scenario.md (line 31) tells agents to pass exactly these unnamespaced ids — either scenario should be updated or the CLI should accept bare ids with a default namespace. Friction is small (re-ran with `topic:freewill`) but every new agent will hit it on their first command.
- (struck) `--types=thought --since=24h` returning empty was a false alarm — no thoughts existed yet at the time. Verified working once posts landed.
- `rufio reason --decision=<hypothesis-id>` rejected with a clear error explaining that --decision must point to a decision-type thought, and offering the right next step (`think --type=decision` or `think --parent=`). Severity: LOW. Error UX was actually excellent here — flagging only because the scenario.md (line 34) says "Refute or confirm with reasoning chains via `rufio reason --decision=<id>`" which implies you reason directly on peer hypotheses. Either scenario should mention the hypothesis→decision elevation step, or `reason` should accept hypothesis parents (less safe). Current behaviour is defensible; scenario wording is slightly misleading.
- `rufio recall --types=thought --since=15m` does NOT surface `reason` records, even though reasoning is part of the cognitive substrate per the scenario. Severity: HIGH. I missed FIVE substantive contributions from claude-gamma (3t9j7n, 0vam5r, cuzlsl, ik7hug, w3u024, ffjvuv) over ~10 minutes because the user-prompt told me to filter `--types=thought` and reason records require `--types=reason` separately. I incorrectly concluded "gamma silent" when gamma was the most active contributor. Almost caused me to draft a quorum decision missing gamma's voice. Fix options: (a) default `--types=thought` should also include reason (treat as thought-subtype); (b) require an explicit `--exclude-reason` flag to hide them; (c) emit a hint when reason records exist outside the requested filter ("note: 5 reason records exist in window but were filtered out — use --types=reason"). Option (c) is the least breaking. THIS IS LAUNCH-RELEVANT because the cognition workflow naturally produces reason records and any agent doing recall during a long-running coordination will hit this.
- Related to above: scenario.md (line 32) says "rufio recall --types=thought --since=24h — see what your peers are thinking before you contribute" — this is the exact command that swallows reason records. Either scenario should say `--types=thought,reason` or the default behaviour should change.
- Confirmation: `--types=thought` ALSO swallows confirm and refute records, not just reason. I had missed FOUR claude-beta confirms (1xujyn, 08iugj, x61rag, 8x7zlp) and ONE claude-gamma refute (on my opening clause (2), posted before beta's similar objection) — all invisible under the recommended filter. The confirm and refute records were the most important coordination signals available, since the scenario's auto-promote logic is driven by confirm-quorum. Severity escalation: this is closer to LAUNCH-BLOCKER for any long-horizon multi-agent run. Workaround discovered: omit --types entirely, or run separate calls per type (`--types=confirm`, `--types=refute`, `--types=reason`, `--types=thought`). The default `rufio recall --since=Xm` (no type filter) returns everything and is what I should have been using all along.
- Adjacent observation (minor): the confirms file (live/confirms/<target-id>.gdl) reuses the target id as the file basename, so multiple confirms on one target append to the same file. The `recall --types=confirm` summary line then prints the SAME id in both the 'id' and 'target=' columns, which is briefly confusing. Severity: LOW. Cosmetic — easy to read once you know.
- The directory `live/confirms/` holds BOTH @confirm AND @refute records — e.g. tko8qi.gdl in that dir is actually a refute (claude-gamma refuting my opening clause (2)). Dir naming is misleading: a developer browsing the filesystem to understand "what got confirmed" will accidentally count refutes as confirms. The `rufio recall --types=refute` filter works correctly, so this is filesystem-layout only, not CLI-behavior. Severity: LOW-MED. Either rename dir to `live/votes/` or split into `live/confirms/` and `live/refutes/`.
- Substrate cannot compel an unresponsive agent to wake. Auto-promote requires all-N confirms per scenario.md, but if one agent stops being invoked, promotion is blocked indefinitely even with strong-majority endorsement and zero refutes. Severity: LAUNCH-BLOCKER for any multi-agent quorum use case. Possible fixes: (a) timeout-with-majority quorum policy, (b) explicit summon-to-confirm flow surfaced as a CLI verb, (c) documented expectation that quorum requires synchronized wake cycles. Currently the substrate produces a 'silent agents are vetoing agents' default which is exactly backwards from human consensus norms.
